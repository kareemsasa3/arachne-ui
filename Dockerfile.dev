# services/arachne-ui/Dockerfile.dev
# Next.js dev server with the same simple strategy: root user, host-mounted code.

FROM node:20

ENV NODE_ENV=development

WORKDIR /app

# Next.js dev server; compose will pass host/port
CMD ["npm", "run", "dev"]
